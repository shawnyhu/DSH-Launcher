# DSH Launcher 项目说明（Windows 现状与 macOS 版本实施规范）

> 文档状态：可直接作为 macOS 版本的需求说明、技术设计和实施任务书。
> 当前 Windows 版本：`0.1.9`。
> 目标仓库：[`shawnyhu/DSH-Launcher`](https://github.com/shawnyhu/DSH-Launcher)。
> 上游项目：[`deepseek-ai/deepseek-harness`](https://github.com/deepseek-ai/deepseek-harness)。
> 最后核对日期：`2026-08-21`。上游 DSH 最新 Release 为 `v0.1.1-rc.1`，仍处于开发者预览阶段。

## 0. Mac 开发接手入口

### 0.1 当前仓库基线

- `main` 已包含 Windows `0.1.9` 的源码、CI、安装器和 Release 工作流。
- Mac 工程、`macos-ci.yml` 和 `macos-release.yml` 尚未创建；Mac 开发从 `macos/` 新目录开始。
- Windows 源码保留在 `src/DshLauncher/`，安装器在 `installer/`，构建脚本在 `scripts/`。Mac 开发不得移动、批量格式化或重写这些目录。
- Windows 与 Mac 从跨平台阶段开始共用产品版本号，仍使用独立的平台标签、Release、安装包和更新通道。
- Windows `0.1.9` 是跨平台开发前的历史基线。第一个双平台共同版本定为 `0.2.0`。
- Mac 首版优先支持 Apple Silicon 和 macOS 13+；Intel 构建属于后续可选项。

### 0.2 在 Mac 上开始工作

```bash
git clone https://github.com/shawnyhu/DSH-Launcher.git
cd DSH-Launcher
git switch main
git pull --ff-only
xcodebuild -version
swift --version
node --version
npm --version
```

创建 `mac/<feature-name>` 分支，所有 Mac 工程文件放入 `macos/`。第一批提交只完成工程骨架、菜单栏入口、设置存储、日志和单元测试，暂不加入发布证书或真实发布动作。

### 0.3 第一批 PR 的完成标准

- `macos/DSHLauncher.xcodeproj` 能在当前稳定版 Xcode 中构建。
- 应用以菜单栏程序运行，`LSUIElement` 为 `true`，默认不显示 Dock 图标。
- 能打开和关闭一个纵向、可缩放的配置窗口。
- 建立 `LauncherSettings`、`SettingsStore`、`AppPaths`、`AppLogger` 和依赖注入边界。
- 设置文件采用原子写入；损坏配置会备份并回退到安全默认值。
- 单元测试不依赖真实 npm、GitHub、通知权限或管理员权限。
- 新增 `macos/README.md`、`macos/CHANGELOG.md` 和只执行构建/测试的 `macos-ci.yml`。
- CI 不读取发布证书、Apple 凭据或更新签名私钥。

### 0.4 Mac `0.2.0` 发布完成标准

Mac `0.2.0` 需要完成本文件第 12 节的 Apple Silicon 验收项目，并满足以下条件：

1. 在没有安装 DSH 的普通用户环境中完成首次配置和 DSH 安装。
2. 能管理全局 DSH、多个独立 DSH 版本和多个 `DSH_HOME`。
3. 能可靠启动、停止、重启、完成就绪检测，并且只打开一次 Web UI。
4. 菜单栏状态、提问/审批通知、完成通知和断线重连可用。
5. Mac Launcher 只发现 `mac-v*`，不会读取 Windows 更新资产。
6. Windows 与 Mac 均显示 `0.2.0`；`win-v0.2.0` 和 `mac-v0.2.0` 尽量指向同一个 `main` 提交。
7. 正式 `.app` 和 `.pkg` 使用 Developer ID 签名、Hardened Runtime 和 Apple 公证。
8. 在没有 Xcode 和开发证书的干净 Apple Silicon Mac 上完成安装与更新验收。

本地开发版本可以使用 ad-hoc 签名或 Xcode 开发签名。公开 Release 必须满足正式签名和公证要求。

## 1. 项目是什么

DSH Launcher 是 DeepSeek Harness（DSH，npm 包名 `@deepseek-ai/dsh`）的桌面安装、启动和管理工具。它常驻系统托盘或 macOS 菜单栏，负责管理 DSH 程序包、DSH 数据目录、Web 服务进程、状态通知以及 Launcher 自身更新。

DSH Launcher、DSH 程序包和 `DSH_HOME` 是三个相互独立的对象：

1. **DSH Launcher**：桌面管理程序。Windows 上是 `DshLauncher.exe`，macOS 上是 `DSH Launcher.app`。
2. **DSH 程序包**：`@deepseek-ai/dsh`。可以全局安装，也可以由 Launcher 在独立目录中安装多个版本。
3. **DSH_HOME**：DSH 的用户数据根目录，保存配置、会话、插件和工作数据。默认是用户目录下的 `~/.dsh`，也允许添加和切换其他目录。

卸载或更新 Launcher 不应修改 DSH 程序包和 `DSH_HOME`。卸载、更新或重装某个 DSH 程序包也不应删除 `DSH_HOME`。

## 2. 产品边界和安全原则

### 2.1 Launcher 负责的内容

- 在新电脑上安装并配置运行 DSH 所需的环境。
- 发现、安装、选择、更新、重装和卸载 DSH 程序包。
- 管理多个 DSH 版本，包括一个 npm 全局版本和多个独立安装版本。
- 管理多个 `DSH_HOME`，并记录 Launcher 观察到的最后写入版本。
- 启动、停止、重启 Launcher 自己创建的 DSH 进程。
- 打开 DSH Web UI，默认地址为 `http://127.0.0.1:3080`。
- 监控 DSH 会话、任务、提问和权限审批状态。
- 改变菜单栏图标并发送系统通知。
- 检查并安装 Launcher 自身更新。

### 2.2 Launcher 不应做的内容

- 不读取、保存、显示、上传或转发用户的 API Key。
- 不实现 API 余额查询。
- 不删除 `DSH_HOME` 中的配置、凭据、插件、会话和工作数据。
- 不强制结束 Launcher 没有启动的 DSH 进程。
- 不把 Web 服务绑定到局域网地址；默认且推荐只监听 `127.0.0.1`。
- 不依赖旧的 DshTray，也不迁移或控制 DshTray。

## 3. 当前 Windows 版本已经实现的能力

Windows `0.1.9` 已实现以下功能，macOS 版应以相同行为作为基准：

- 托盘常驻，双击图标打开 DSH Web UI。
- 启动、停止、重启 Launcher 管理的 DSH。
- 当前版本、当前 `DSH_HOME`、当前端口和运行状态展示。
- 多个 DSH 安装实例管理。
- npm 全局安装和 Launcher 管理的独立目录安装。
- 查询所有可安装版本，并安装指定旧版本或最新版本。
- 更新、重装和卸载选中的 DSH。
- DSH 更新按官方 GitHub Release 发布时间判断，GitHub API 受限时使用 Atom Feed，再退回 npm 发布时间。
- 管理多个 `DSH_HOME`，显示最后由哪个 DSH 版本写入。
- 状态图标：停止、空闲、运行、等待用户处理、不兼容。
- 提问、权限审批和对话完成通知。
- 配置端口、工作目录、启动行为、通知和登录时启动。
- 从 GitHub Release 检查 Launcher 更新。
- 更新 DSH 或 Launcher 时显示进度窗口。
- Launcher 更新包在下次正常启动后清理。

Windows 实现使用 C#、.NET 10 和 WinForms。WinForms、Windows Job Object、注册表自启动、Inno Setup 和 Windows 通知等实现不能直接移植到 macOS。

## 4. macOS 版本的用户体验

### 4.1 第一次安装

推荐发布一个签名并公证的完整安装包：

```text
DSHLauncher-Mac-Setup-<version>-arm64.pkg
```

可选提供 Intel 版本：

```text
DSHLauncher-Mac-Setup-<version>-x64.pkg
```

安装包把 `DSH Launcher.app` 安装到 `/Applications`。用户第一次打开 Launcher 时进入初始化向导：

1. 检查当前 CPU 架构和 macOS 版本。
2. 检查 Node.js；目标使用 Node.js 24 LTS。上游仓库当前要求 `^22.19.0 || >=24.0.0`，安装前必须按第 5.2 节重新验证。
3. 未发现兼容 Node.js 时，引导安装经过哈希校验的 Node.js 官方安装包。不得静默执行来源不明的脚本。
4. 默认选择“全局安装 DSH”，同时允许改为 Launcher 管理的独立目录。
5. 选择要安装的 DSH 版本，默认使用最新的官方可安装 Release。
6. 选择 `DSH_HOME`，默认填入当前用户的 `~/.dsh`，必须允许浏览和手动输入路径。
7. 完成后保存配置。安装器和更新器不自动启动 DSH；用户从菜单栏主动启动，避免安装权限和桌面用户会话发生冲突。

`.pkg` 的安装脚本以 root 身份运行，不能在脚本中直接创建用户的 `~/.dsh` 或用户配置，否则容易产生错误的文件所有者。用户目录配置应由首次以普通用户身份运行的 Launcher 创建。

### 4.2 菜单栏图标

使用 DSH 自带的黑白鲸鱼图标。图标状态：

| 状态 | 图标 | 判定 |
|---|---|---|
| Stopped | 灰色鲸鱼 | DSH 未运行或端口未就绪 |
| Idle | 系统模板色鲸鱼 | DSH 已运行，无会话和任务活动 |
| Busy | 绿色鲸鱼 | 有会话或后台任务正在运行 |
| Attention | 黄色闪烁鲸鱼 | 有待回答问题或待审批权限 |
| Incompatible | 橙色鲸鱼 | 当前 DSH 与 `DSH_HOME` 存在已知版本风险 |

macOS 空闲图标应使用 `NSImage.isTemplate = true`，由系统自动适配浅色和深色菜单栏。绿色、黄色、灰色和橙色状态使用非模板彩色图标。Attention 状态建议以约 500 毫秒间隔在黄色和半透明黄色之间切换。

图标资源应从 DeepSeek Harness 仓库中的正式资源派生，并保留来源与许可证记录。DeepSeek Harness 已发布品牌使用规范，公开版本必须在 README 或 NOTICE 中说明 DSH Launcher 是社区项目，避免暗示 DeepSeek 官方背书。图标的状态着色只用于表达本机运行状态，不修改主体轮廓或增加误导性品牌组合。

### 4.3 菜单栏菜单

建议菜单顺序：

```text
DSH Launcher <Launcher 版本>
当前 DSH：<版本>（全局/独立）
DSH_HOME：<路径>
────────────────────────
打开 DSH 网页
启动 DSH
停止 DSH
重启 DSH
────────────────────────
检查并更新当前 DSH
检查 Launcher 更新
配置…
登录时启动  ✓
────────────────────────
退出并停止 DSH
```

菜单栏图标的主要点击行为可直接打开菜单。由于 macOS 菜单栏项目没有统一可靠的“双击”交互习惯，建议同时支持：

- 左键单击：打开菜单。
- `Option` + 单击或菜单中的“打开 DSH 网页”：启动 DSH（如需要）并打开网页。
- 可在设置中提供“单击图标直接打开网页”的可选行为，但默认遵循 macOS 菜单栏习惯。

### 4.4 配置窗口

使用纵向、可缩放布局，避免窄窗口中按钮被裁切。推荐 macOS 原生侧边栏结构：

- **DSH 版本**
  - 安装实例列表。
  - 选中实例的版本、类型、安装路径、Node 路径和兼容性信息。
  - 安装版本、检查更新、重装、卸载、设为当前版本。
- **DSH_HOME**
  - 已记录的数据目录列表。
  - 添加已有目录、创建目录、移除记录、设为当前目录。
  - 显示最后观察到的写入版本、时间和记录可靠性。
- **启动设置**
  - 端口，默认 `3080`。
  - 工作目录，默认当前用户目录。
  - Launcher 启动时启动 DSH。
  - DSH 启动后打开浏览器。
  - 对话完成通知。
  - 登录时启动。
  - Launcher 更新仓库，默认 `shawnyhu/DSH-Launcher`。

底部按钮纵向适配窗口宽度，至少提供“保存”“保存并重启 DSH”“关闭”。运行配置发生变化且 DSH 正在运行时，应明确询问或执行重启。

### 4.5 安装指定 DSH 版本

点击“安装版本”打开独立窗口：

1. 查询 npm 的全部 `@deepseek-ai/dsh` 版本。
2. 使用 DeepSeek Harness 官方 GitHub Release 发布时间排序。
3. 支持版本号搜索。
4. 选择安装范围：全局或 Launcher 管理的独立目录。
5. 独立目录允许手动选择路径，默认目录为：

```text
~/Library/Application Support/DSHLauncher/runtimes/<version>
```

6. 显示将执行的版本、路径和空间范围，确认后开始安装。

npm 全局范围在同一个 npm prefix 中只能保留一个版本。需要同时保留多个版本时必须使用不同的独立目录。

### 4.6 更新进度窗口

更新 DSH 和更新 Launcher 都必须弹出独立小窗口。更新执行期间不允许重复发起另一项更新。

Launcher 更新可以获取 HTTP `Content-Length` 时显示真实下载字节和百分比；没有总大小时显示循环进度条。DSH 的 npm 安装通常不能提供可靠的总字节，应显示当前阶段、npm 输出摘要和循环进度条。

DSH 更新阶段：

```text
检查版本 → 停止当前 DSH → 安装程序包 → 验证版本 → 保存配置 → 按原状态恢复运行
```

Launcher 更新阶段：

```text
检查版本 → 下载 → 校验签名/哈希 → 安装 → 提示重新启动
```

失败时保留清晰错误信息并删除未完成下载。正式安装完成后由更新框架清理缓存。

## 5. DSH 的技术要求

### 5.1 官方运行方式

DeepSeek Harness 官方仓库目前把 DSH 定义为开发者预览版，并提示可能发生破坏兼容性的变化。官方最小运行入口为：

```bash
npx @deepseek-ai/dsh web
```

Web UI 默认运行在：

```text
http://127.0.0.1:3080
```

Launcher 应直接启动所选安装中的 Node 和 DSH JavaScript 入口，避免依赖交互式 shell、`nvm` 初始化脚本或 npm shim。启动参数：

```text
<node> <dsh-entry> web --host 127.0.0.1 --port <port> --no-open
```

环境变量：

```text
DSH_HOME=<所选数据目录的绝对路径>
```

必须保证子进程的 `PATH` 中可以找到对应 Node.js。DSH 的子进程会继续通过环境查找 `node`；只保存入口文件路径但不给子进程正确 `PATH`，可能导致 Web UI 能启动、创建会话却失败。

### 5.2 Node.js

- 产品目标使用 Node.js 24 LTS。
- 上游仓库在 `2026-08-21` 声明的 Node 范围为 `^22.19.0 || >=24.0.0`。
- 安装或更新 DSH 前先执行 `npm view @deepseek-ai/dsh@<version> engines --json`。已发布的 DSH 包目前可能没有携带 `engines`；空结果不能解释为兼容所有 Node。
- 包未声明 `engines` 时，读取 DeepSeek Harness 官方仓库根 `package.json` 的 `engines.node`。网络不可用时使用 Launcher 内置的最近一次已验证范围，并在界面中注明来源和核对日期。
- npm 的 engine 检查通常只是警告。Launcher 必须自行解析范围并阻止已知不兼容的 Node 启动 DSH。
- 必须同时保存并验证 Node 和 npm 的真实路径。
- Apple Silicon 常见 Homebrew 路径为 `/opt/homebrew/bin`，Intel 常见路径为 `/usr/local/bin`，但代码不能硬编码这些路径。
- 从 Finder 启动的 GUI 应用不会继承用户交互式 shell 的完整 `PATH`。需要组合系统安全路径、Node 所在目录和 npm prefix 的 bin 目录。
- 支持 Homebrew、官方 Node 安装包、nvm、fnm、Volta 等来源的“发现”，但 Launcher 管理的配置必须固定到已验证的绝对路径，避免版本管理器切换后静默改变运行时。

### 5.3 全局 DSH 发现

依次执行：

```bash
npm root --global
npm prefix --global
```

程序包目录通常是：

```text
<npm-global-root>/@deepseek-ai/dsh
```

不能假设全局 npm 一定位于 `/usr/local` 或 `/opt/homebrew`。如果 npm 全局 prefix 对当前用户不可写，安装前显示管理员权限提示。不要把 `sudo` 密码通过标准输入传给子进程，也不要记录权限命令中的敏感环境变量。

### 5.4 Launcher 管理的独立安装

每个版本使用独立根目录。安装命令应等价于：

```bash
npm install --prefix <install-root> --no-package-lock --no-save @deepseek-ai/dsh@<version>
```

安装完成后验证：

- `package.json` 中的实际版本等于目标版本。
- DSH CLI 入口存在。
- Node 可执行文件存在并与当前 CPU 架构匹配。
- 运行 `<node> <entry> --version` 成功。
- 写入 Launcher 自己的实例归属文件，例如 `.dsh-launcher-instance.json`，避免误删用户手动维护的目录。

卸载独立实例时，只允许删除带有效归属文件且解析后的绝对路径位于 Launcher 管理根目录或用户明确选择并再次确认的目录。不得通过未校验的字符串路径递归删除。

### 5.5 DSH_HOME

DSH 官方路径解析优先级为：显式配置、`$DSH_HOME`、`~/.dsh`。默认目录：

```text
~/.dsh
```

该目录可能包含：

```text
settings.yaml
cordis.patch.yml
.credentials.yaml
profiles/
sessions/
```

Launcher 只管理目录路径和兼容性元数据。文件监听时排除 `.credentials.yaml`，日志也不能输出该文件内容。

同一个 `DSH_HOME` 可能被不同 DSH 版本读取。Launcher 记录以下字段：

- `lastObservedWriterVersion`
- `lastObservedWriteAt`
- `observationReliable`

切换到与最后写入版本不同的 DSH 时显示兼容性警告。该记录来自 Launcher 运行期间的文件事件，不能当作绝对可靠的版本迁移证明。

### 5.6 DSH 版本与 Release 映射

DSH 的 npm 版本、GitHub Release 标题和 Git tag 并非同一种字符串。当前官方 Release 展示为 `v0.1.1-rc.1`，关联标签为 `dsh-v0.1.1-rc.1`，npm 包版本为 `0.1.1-rc.1`。Launcher 应把它们归一化到同一个包版本后再匹配。

更新判断规则：

1. 从 DeepSeek Harness 官方 GitHub Release 列表读取 `published_at`，排除 draft，并保留用户更新渠道允许的 prerelease。
2. 从 Release 标签解析 DSH 包版本，确认 npm registry 中存在完全相同的 `@deepseek-ai/dsh@<version>`。
3. 以发布时间选择当前安装之后发布的最新可安装 Release。不要用普通字符串或不完整的 SemVer 比较决定 DSH 的“新旧”。
4. GitHub API 限流时使用官方 Releases Atom Feed；Feed 也不可用时才使用 npm 版本发布时间。
5. Release 已发布但 npm 包尚未同步时显示“官方 Release 尚不可安装”，不得安装另一个近似版本。

每次开始实现 DSH 适配前，先重新核对上游 README、根 `package.json`、CLI 参数、Release 标签和事件协议。文档中的当前版本只代表 `2026-08-21` 的已验证基线。

## 6. DSH 运行与状态监控协议

### 6.1 就绪检测

启动 DSH 后最多等待 60 秒，循环检查回环地址上的服务。进程提前退出时显示退出码和 stderr 中最后一条有效错误。只有就绪后才打开浏览器。

浏览器由 Launcher 统一打开，DSH 启动必须带 `--no-open`，否则容易出现两个网页。

### 6.2 实时事件

使用两个 WebSocket：

```text
ws://127.0.0.1:<port>/api/events.mux
ws://127.0.0.1:<port>/api/events.host
```

首次连接后通过以下 RPC 建立运行基线：

```text
POST http://127.0.0.1:<port>/api/session.list
```

主要事件：

| 事件 | 用途 |
|---|---|
| `host/session-status` | 更新会话 `running` 状态 |
| `question/requested` | 进入 Attention，发送提问通知 |
| `question/resolved` | 清除对应待回答项 |
| `approval/requested` | 进入 Attention，发送审批通知 |
| `approval/resolved` | 清除对应待审批项 |
| `session/event` 中的 `turn/start` | 标记会话运行 |
| `session/event` 中的 `turn/end` | 标记完成并发送完成通知 |
| `session/jobs` | 统计 running/stopping 后台任务 |
| `session/projection` 的 `title` | 保存会话标题，用于通知文案 |

状态优先级：

```text
Attention > Busy > Idle > Stopped
```

重连采用约 2 秒到 15 秒的指数退避。事件流空闲时可能没有心跳，应设置死链检测并重连。首次连接重放的挂起提问和审批可以通知；网络重连产生的重复基线不得反复通知。对话完成通知建议在 3 秒窗口内合并。

### 6.3 系统通知

使用 `UNUserNotificationCenter`：

- 提问：标题“DSH 需要你的回答”，正文包含问题标题或问题文本。
- 审批：标题“DSH 请求权限审批”，正文包含工具名和原因。
- 完成：标题“对话完成”，正文优先使用会话标题。
- 点击通知：启动 DSH（如需要）并打开当前端口的 Web UI。

首次运行应解释通知用途并请求系统授权。用户拒绝后应用继续工作，只在配置页显示通知权限状态。

## 7. macOS 推荐技术架构

### 7.1 技术栈

- **语言**：Swift 6。
- **最低系统**：macOS 13 Ventura。
- **界面**：SwiftUI 管理配置和安装窗口，AppKit 管理 `NSStatusItem`、菜单和需要精细控制的窗口行为。
- **网络**：`URLSession`、`URLSessionWebSocketTask`。
- **进程**：Foundation `Process`，配合 POSIX process group 和信号处理。
- **通知**：`UserNotifications`。
- **登录启动**：macOS 13+ 使用 `SMAppService.mainApp`。
- **更新**：自定义 GitHub Release 更新服务；Windows 与 Mac 使用同一检测协议，按平台标签、版本和架构选择资产。
- **配置**：`Codable` JSON，原子写入。
- **测试**：Swift Testing 或 XCTest；核心服务通过 protocol 注入命令、文件系统、网络和时钟。

选择 Swift 原生实现可以得到可靠的菜单栏、通知、登录启动、代码签名和公证体验。Windows 的业务规则与更新协议可以复用设计和测试用例，平台 UI、安装程序与系统服务需要重写。

### 7.2 建议模块

```text
DSHLauncherMac
├── App
│   ├── DSHLauncherApp
│   ├── AppDelegate
│   └── MenuBarController
├── Models
│   ├── LauncherSettings
│   ├── DSHInstallation
│   ├── DSHHomeEntry
│   ├── DSHStatusSnapshot
│   └── OperationProgress
├── Services
│   ├── CommandRunner
│   ├── NodeDiscoveryService
│   ├── NpmService
│   ├── DSHReleaseService
│   ├── DSHRuntimeService
│   ├── DSHEventMonitor
│   ├── DSHHomeWatcher
│   ├── PortService
│   ├── LoginItemService
│   ├── NotificationService
│   └── LauncherUpdateService
├── Views
│   ├── ConfigurationView
│   ├── InstallVersionView
│   ├── FirstRunView
│   └── UpdateProgressView
├── Infrastructure
│   ├── AppPaths
│   ├── SettingsStore
│   ├── AppLogger
│   └── SingleInstanceCoordinator
└── Resources
    ├── Assets.xcassets
    └── Whale.svg
```

### 7.3 macOS 数据路径

```text
应用：/Applications/DSH Launcher.app
设置：~/Library/Application Support/DSHLauncher/settings.json
托管 DSH：~/Library/Application Support/DSHLauncher/runtimes/
日志：~/Library/Logs/DSHLauncher/launcher.log
缓存：~/Library/Caches/DSHLauncher/
默认 DSH_HOME：~/.dsh
```

Launcher 更新下载必须限制在 `~/Library/Caches/DSHLauncher/updates/`，启动时只清理经过绝对路径校验的该目录。未完成下载使用临时扩展名，失败后立即删除。

### 7.4 进程所有权

Launcher 只保存和停止自己创建的 DSH `Process`。启动时创建独立 process group；正常停止先发送 `SIGTERM`，等待有限时间，再对该 process group 发送 `SIGKILL`。退出 Launcher 时停止其管理的 DSH。

如果目标端口已经有可用 DSH，但 Launcher 没有该进程句柄，可以显示“检测到外部 DSH”，允许打开网页和只读监控，禁止强制停止、重启或更新，直到用户自行关闭外部进程。

### 7.5 单实例

只允许一个 Launcher 菜单栏实例。第二次打开应用时向已运行实例发送“打开网页”或“显示配置”请求，然后退出。可以使用 `NSRunningApplication`、自定义 URL Scheme 或本机 Distributed Notification 实现；通信内容不能包含 API Key 和 `DSH_HOME` 文件内容。

### 7.6 应用身份与发布边界

建议从第一天固定以下标识，后续变更会影响登录项、通知授权和代码签名：

```text
产品名：DSH Launcher
Bundle Identifier：com.shawnyhu.dshlauncher
最低系统：macOS 13.0
首发架构：arm64
菜单栏应用：LSUIElement = true
正式分发：Developer ID，仓库 Release，不进入 Mac App Store
```

Launcher 需要启动用户选择目录中的 Node、npm 和 DSH，并管理非沙盒工作目录。第一版启用 Hardened Runtime，暂不启用 App Sandbox。Entitlements 保持最小化，禁止把 `get-task-allow` 加入正式构建。

本地开发、CI 构建和正式发布分三层：

| 层级 | 签名 | 可执行内容 | 允许发布 |
|---|---|---|---|
| 本地 Debug | Xcode Development 或 ad-hoc | 完整功能，可使用模拟服务 | 否 |
| PR CI | ad-hoc 或不需要身份的测试签名 | 构建、单测、自检，不访问发布 Secrets | 否 |
| Release | Developer ID Application + Developer ID Installer | 完整功能，Hardened Runtime，公证并 staple | 是 |

### 7.7 Swift 并发与服务边界

- AppKit、SwiftUI 状态、窗口和 `NSStatusItem` 更新统一放在 `@MainActor`。
- DSH 进程、WebSocket、文件监听、下载和 npm 操作通过独立 actor 或明确的串行队列管理。
- `DSHRuntimeService` 只有一个启动/停止状态机；启动、停止、重启和更新不能并发进入。
- 长任务使用 Swift 结构化并发和 `Task` 取消，并在界面关闭或应用退出时传播取消。
- `Process` 的 stdout/stderr 必须持续异步排空，避免管道缓冲区写满导致子进程挂起。
- 服务向 UI 发送不可变、`Sendable` 的状态快照。UI 不直接持有可变进程句柄或 WebSocket 状态。
- 通知、浏览器打开和文件删除作为可注入接口；单元测试使用 fake，不产生真实系统副作用。

### 7.8 设置迁移和故障恢复

`settings.json` 必须包含 `schemaVersion`。读取流程：

1. 读取并解码当前文件。
2. 按版本逐级迁移，不允许跳过中间迁移。
3. 写入同目录临时文件，`fsync` 后原子替换正式文件。
4. 成功后保留一个不含凭据的最近备份。
5. 解码或迁移失败时，把原文件改名为带时间戳的 `.corrupt` 备份，加载安全默认值并向用户说明。

路径在保存前展开 `~`、标准化并尽可能解析符号链接。删除、卸载和缓存清理前必须再次解析绝对路径并验证目录边界。设置中只保存 DSH 路径、版本、用户选项和必要的 security-scoped bookmark；不得复制 `DSH_HOME` 内容或凭据。

## 8. 设置数据模型

建议保持与 Windows 逻辑兼容，但不要直接复制 Windows 可执行文件路径。示例：

```json
{
  "schemaVersion": 1,
  "installations": [
    {
      "id": "UUID",
      "name": "Global DSH",
      "scope": "global",
      "installRoot": "/opt/homebrew",
      "packageRoot": "/opt/homebrew/lib/node_modules/@deepseek-ai/dsh",
      "nodeExecutable": "/opt/homebrew/bin/node",
      "npmExecutable": "/opt/homebrew/bin/npm",
      "installedVersion": "0.1.1-rc.1",
      "lastVerifiedAt": "2026-08-21T12:00:00Z"
    }
  ],
  "homes": [
    {
      "id": "UUID",
      "name": "Default Data",
      "path": "/Users/example/.dsh",
      "lastObservedWriterVersion": "0.1.1-rc.1",
      "lastObservedWriteAt": "2026-08-21T12:10:00Z",
      "observationReliable": true
    }
  ],
  "selectedInstallationId": "UUID",
  "selectedHomeId": "UUID",
  "port": 3080,
  "workingDirectory": "/Users/example",
  "startDshWithLauncher": true,
  "openBrowserAfterStart": true,
  "startAtLogin": false,
  "notifyOnCompletion": true,
  "launcherUpdateRepository": "shawnyhu/DSH-Launcher"
}
```

Windows 与 macOS 不应共享同一个本机 `settings.json`。两端可以共享字段定义、迁移规范和测试样例。

## 9. GitHub 仓库共存方案

Windows 和 macOS 代码放在同一个公开仓库中。从 `0.2.0` 起共用面向用户的产品版本号，平台源码、构建、标签、Release 和安装包分别维护。

### 9.0 为什么共用仓库

该方案符合成熟跨平台桌面项目的常见做法：

| 项目 | 同仓库平台 | 可借鉴点 |
|---|---|---|
| [GitHub Desktop](https://github.com/desktop/desktop) | Windows、macOS | 同一产品入口、共享 Issue 与构建规范 |
| [Visual Studio Code](https://github.com/microsoft/vscode) | Windows、macOS、Linux | 同仓库管理平台构建与公共功能 |
| [KeePassXC](https://github.com/keepassxreboot/keepassxc) | Windows、macOS、Linux | 同一 Release 体系提供多平台安装资产 |
| [RustDesk](https://github.com/rustdesk/rustdesk) | 桌面与移动多平台 | 同仓库维护大量平台构建和架构资产 |

DSH Launcher 的产品边界、DSH 协议、隐私规则、数据模型和用户支持入口高度一致。共用仓库可以让协议修改、Issue、文档和安全修复同时覆盖两个平台。原生 UI 与系统集成通过目录、CI 路径过滤和平台标签隔离。

以下情况长期出现时再评估拆仓库：

- 两个平台由完全独立的团队和发布权限管理。
- 共享协议与产品需求很少再发生共同变化。
- 单仓库 CI 成本或权限隔离已经成为持续阻碍。
- Issue、里程碑和安全响应需要不同治理流程。

当前项目不满足这些拆分条件，继续使用同一个仓库。

### 9.1 推荐目录结构

```text
DSH-Launcher/
├── src/DshLauncher/                 # 现有 Windows 源码
├── installer/                       # 现有 Windows Inno Setup
├── scripts/                         # Windows 构建脚本
├── macos/
│   ├── DSHLauncher.xcodeproj
│   ├── DSHLauncher/
│   ├── DSHLauncherTests/
│   ├── Packaging/
│   ├── Scripts/
│   ├── README.md
│   └── CHANGELOG.md
├── .github/workflows/
│   ├── windows-ci.yml
│   ├── windows-release.yml
│   ├── macos-ci.yml
│   └── macos-release.yml
├── README.md                         # 两个平台的总入口
├── CHANGELOG.md                      # Windows 历史可暂时保留
└── 项目说明.md
```

Mac Codex 应在 `macos/` 下创建工程，避免移动或重写现有 Windows 文件。共享协议文档可以放在 `docs/`，共享测试数据可以放在 `test-fixtures/`。

### 9.2 共享产品版本和平台标签

当前 Windows 已使用 `v0.1.0` 到 `v0.1.8` 的旧标签，并以 `win-v0.1.9` 进入 Windows 平台通道。跨平台版本从 `0.2.0` 开始统一：

| 平台 | 标签格式 | 第一个共同版本 | Release 标题 |
|---|---|---|---|
| Windows | `win-v<version>` | `win-v0.2.0` | `DSH Launcher for Windows v0.2.0` |
| macOS | `mac-v<version>` | `mac-v0.2.0` | `DSH Launcher for macOS v0.2.0` |

版本规则：

1. `MARKETING_VERSION`、Windows 项目 `Version` 和两个 Release 标题使用相同的 `major.minor.patch`。
2. `CURRENT_PROJECT_VERSION`、Windows 文件构建号和 CI run number 可以按平台独立递增，不面向用户比较。
3. 每次正式发布从同一个 `main` 基线创建 `win-vX.Y.Z` 和 `mac-vX.Y.Z`，两个 Release 可以先后完成。
4. 只修改一个平台时，仍提升共同产品版本并重新发布两个平台；未改动平台只包含版本、依赖和构建更新。
5. 不允许同一平台重复使用已经发布的版本号指向不同源码。
6. 预发布版本暂时通过手动工作流或 Draft Release 测试。等两个更新器都支持完整 SemVer prerelease 后，再使用 `0.2.0-beta.1` 一类公开标签。

Windows 更新器只接受 `win-v*`，Mac 更新器只接受 `mac-v*`。两端必须同时校验平台标签、版本号、资产名称、CPU 架构和更新签名或哈希，不能继续把仓库级 `/releases/latest` 当作更新来源。

GitHub 对整个仓库只有一个 “Latest Release”，无法为两个平台各保留一个仓库级 Latest。两个更新器改用平台专用通道后，仓库级 Latest 由 GitHub 指向最后发布的平台版本，只用于网页展示，不参与自动更新判断。

#### 从旧 Windows 标签迁移

现有 Windows `0.1.7` 更新器只能读取 `/releases/latest`，并且只认识 `v<version>`。迁移必须经过一个桥接版本：

1. 先发布 Windows `0.1.8`，仍使用旧标签 `v0.1.8`，并把它设为仓库 Latest。
2. `0.1.8` 更新器新增平台标签检测，能够读取 `win-v*`，同时保留对旧 `v*` 的兼容。
3. 从 Windows `0.1.9` 开始使用 `win-v0.1.9`。
4. Mac Release 使用 `mac-v*`。桥接期间曾需要设置 `--latest=false`，避免仍停留在 `0.1.7` 的客户端读到 Mac Release。
5. 从 Windows `0.1.9` 开始，后续 Release 不再设置 `--latest=false`。GitHub Latest 跟随最后发布的平台版本，自动更新仍不依赖 Latest。

#### 是否合并成一个 GitHub Release

GitHub 的一个 Release 只能关联一个 Git tag。项目虽然共用产品版本号，当前仍保留两个 Release 条目：`win-vX.Y.Z` 只包含 Windows 资产，`mac-vX.Y.Z` 只包含 Mac 资产。这样签名环境、失败重试和平台更新器互不耦合。

未来可以改为一个共同 `vX.Y.Z` 标签和一个多平台 Release，但需要先升级两个 Launcher 的标签解析，并把 Windows 与 Mac 发布编排成同一个受控流程。当前规模下，统一版本号加独立平台 Release 更稳妥。

### 9.3 Release 资产命名

Windows 桥接版本保留旧名称，进入平台通道后使用带平台名称的资产：

```text
DSHLauncher-Setup-0.1.8-x64.exe                  # v0.1.8 桥接版本
DSHLauncher-Update-0.1.8-x64.exe                 # v0.1.8 桥接版本
DSHLauncher-Windows-Setup-0.1.9-x64.exe          # win-v0.1.9 起
DSHLauncher-Windows-Update-0.1.9-x64.exe         # win-v0.1.9 起
```

macOS 使用明确的平台和架构：

```text
DSHLauncher-Mac-Setup-0.2.0-arm64.pkg
DSHLauncher-Mac-0.2.0-arm64.dmg
DSHLauncher-Mac-Update-0.2.0-arm64.pkg
DSHLauncher-Mac-Setup-0.2.0-x64.pkg       # 可选
DSHLauncher-Mac-Update-0.2.0-x64.pkg      # 可选
update-manifest-macos-arm64.json
update-manifest-macos-arm64.sig
checksums-macos.txt
```

完整 `.pkg` 面向首次安装，轻量更新 `.pkg` 只替换 Launcher 应用；`.dmg` 可以作为手动安装备选。Release 附带 SHA-256、更新清单及其数字签名。Mac 更新包还必须通过 Developer ID Installer 签名和 Apple 公证。

### 9.4 GitHub Actions

原有 `.github/workflows/release.yml` 已重命名为 `windows-release.yml`。桥接工作流只接受精确的 `v0.1.8` 和 `win-v*`，不能继续宽泛匹配所有 `v*`；`v0.1.8` 发布完成后保留该精确规则，以便工作流可重跑。

`macos-release.yml` 只匹配：

```yaml
on:
  push:
    tags:
      - "mac-v*"
```

Mac Release 工作流运行在 `macos-latest`，步骤至少包括：

1. Checkout。
2. 选择明确的 Xcode 版本。
3. 解析 `mac-v*` 版本并验证工程版本一致。
4. 运行单元测试。
5. 分别构建 arm64 和可选 x64 产物；包含原生依赖时必须在目标架构上验证。
6. 使用 Developer ID Application 证书签名 `.app`。
7. 开启 Hardened Runtime。
8. 使用 Developer ID Installer 证书签名 `.pkg`。
9. 提交 Apple Notary Service 并等待结果。
10. Staple 公证票据。
11. 运行 `codesign --verify --deep --strict`、`spctl --assess` 和 `stapler validate`。
12. 生成 SHA-256、平台更新清单和清单数字签名。
13. 创建 `mac-v*` GitHub Release，并允许它成为仓库 Latest；Mac 更新器仍按 `mac-v*` 标签选择版本。

需要配置的 GitHub Actions Secrets：

```text
MACOS_APPLICATION_CERTIFICATE_P12
MACOS_APPLICATION_CERTIFICATE_PASSWORD
MACOS_INSTALLER_CERTIFICATE_P12
MACOS_INSTALLER_CERTIFICATE_PASSWORD
APPLE_TEAM_ID
APPLE_ID 或 App Store Connect API Key
APPLE_APP_SPECIFIC_PASSWORD（使用 Apple ID 时）
UPDATE_MANIFEST_PRIVATE_KEY
```

私钥和证书不得提交到仓库。工作流日志不得输出解码后的证书或密码。

### 9.5 CI 与分支策略

- `windows-ci.yml` 在 `windows-latest` 构建现有 .NET 项目。
- `macos-ci.yml` 在 `macos-latest` 构建并测试 `macos/` 工程。
- Pull Request 同时触发两个工作流；通过路径过滤避免无关平台浪费构建时间。
- Mac 功能分支建议使用 `mac/...` 前缀，例如 `mac/bootstrap-menu-bar`。
- 每项功能经 PR 合并到 `main`，发布只通过受保护标签触发。
- macOS Release Notes 写入 `macos/CHANGELOG.md`，总 README 同时链接 Windows 和 macOS 下载入口。

### 9.6 README 和 Issue 模板

仓库根 README 应改成平台入口页，包含：

- Windows 下载、系统要求、安装说明。
- macOS 下载、系统要求、Apple Silicon/Intel 区分、签名与公证状态。
- DSH 与 Launcher 的关系。
- 数据保留和隐私声明。
- 两个平台共用产品版本号、平台构建号独立的说明。

Issue 模板增加必填字段：操作系统、CPU 架构、Launcher 版本、DSH 版本、安装范围、Node 版本。用户提交日志前必须提醒删除凭据和私人会话数据。

### 9.7 公开仓库文件说明

该仓库已经具备公开项目的基础治理文件。Mac 开发应扩展这些文件，避免创建含义重复的第二套根文件。

| 文件或目录 | 当前用途 | 加入 macOS 后的处理 |
|---|---|---|
| `README.md` | Windows 功能、安装、构建和数据说明 | 改为跨平台入口，分别链接 Windows 与 macOS 文档和下载 |
| `CHANGELOG.md` | 现有 Windows 版本历史 | 保留 Windows 历史；Mac 详细记录放入 `macos/CHANGELOG.md`，根文件可增加跨平台摘要 |
| `LICENSE` | MIT 许可证 | 两个平台共同使用，不修改许可证范围 |
| `THIRD_PARTY_NOTICES.md`（如新增） | 第三方依赖、Node 等许可声明 | Mac 发布前补充并随安装包分发 |
| `CONTRIBUTING.md` | 贡献、构建和 PR 规则 | 增加 Xcode、Swift、签名测试和 Mac 分支说明 |
| `CODE_OF_CONDUCT.md` | 社区行为准则 | 两个平台共同使用 |
| `SECURITY.md` | 安全漏洞报告流程 | 增加 macOS 签名、公证、更新签名和权限问题的报告范围 |
| `SUPPORT.md` | 使用支持边界 | 增加 macOS 系统版本、CPU 架构和 Gatekeeper 常见问题 |
| `.gitignore` | 忽略本地产物 | 增加 Xcode DerivedData、`.xcuserstate`、Archives、签名临时文件和更新清单私钥 |
| `.github/pull_request_template.md` | PR 摘要和验证清单 | 增加受影响平台、Mac 签名/测试、DSH_HOME 与凭据检查 |
| `.github/ISSUE_TEMPLATE/` | Bug 和功能请求模板 | 增加平台、架构、macOS 版本、Node 来源等字段 |
| `.github/workflows/` | 当前 Windows CI 与 Release | 拆成清晰命名的 Windows/Mac CI 和 Release；标签互不交叉 |
| `项目说明.md` | 产品边界、跨平台架构和 Mac 实施规范 | 作为两个平台共同的上层规格，重大设计变化应同步更新 |

GitHub 仓库主页建议配置：

- Description：`Cross-platform installer and menu bar/tray manager for DeepSeek Harness.`
- Topics：`deepseek`、`deepseek-harness`、`launcher`、`windows`、`macos`、`swift`、`dotnet`。
- 启用 Issues、Discussions 和 Private vulnerability reporting。
- 保护 `main` 分支，要求对应平台 CI 通过后才能合并。
- 在根 README 和两个平台的 Release Notes 中说明共享产品版本、平台标签规则和仓库级 Latest 不参与自动更新。

### 9.8 Mac 开发的提交和发布流程

一次正常的 Mac 功能开发流程：

```text
main
  └── mac/<feature-name>
        ├── 实现与测试
        ├── 更新 macos/README.md 或 macos/CHANGELOG.md
        └── Pull Request → macOS CI → 合并 main
```

一次正常的双平台发布流程：

1. 决定共同产品版本，例如 `0.2.0`。
2. 更新 Windows 项目 `Version`、Mac `MARKETING_VERSION`，并分别更新平台构建号。
3. 更新根 `CHANGELOG.md` 和 `macos/CHANGELOG.md`。
4. 合并发布准备 PR，确保 Windows CI 与 macOS CI 都通过。
5. 从同一个 `main` 提交创建 `win-v<version>` 和 `mac-v<version>` annotated tag。
6. 分别推送标签，让两个平台工作流独立构建和发布。
7. 验证 Windows Release 与 Mac Release 的版本一致，资产、哈希和签名均正确。
8. 在没有开发工具的普通 Windows 和 Apple Silicon Mac 上执行安装与更新验收。

Mac Codex 可以向同一个远程仓库提交和推送，但不能重写已有标签，也不能让 `mac-v*` 触发 Windows Release 工作流。Mac 更新器必须拒绝 `win-v*` 和旧 `v*`。第一次创建 Mac Release 前，先通过 `workflow_dispatch` 的非发布模式验证产物；发布 Secrets 只提供给受保护的 Release environment。

## 10. Launcher 自身的统一更新协议

Windows 和 macOS 共享更新发现状态机和平台隔离规则。语言与安装方式可以不同，标签解析、候选版本选择、资产唯一性、下载状态和失败语义必须由同一组测试样例约束。

### 10.0 当前实现与 `0.2.0` 目标

Windows `0.1.9` 的当前实现已经具备：

- GitHub Releases API 查询，匿名 API 限流或网络异常时退回官方 Atom Feed。
- 严格接受 `win-v*`，并为 Windows 桥接保留到 `v0.1.8` 为止的旧标签。
- 按版本选择唯一的 Windows x64 更新资产，校验 GitHub 提供的文件大小。
- 流式下载、进度反馈、失败清理，以及关闭下载文件句柄后再重命名。

Windows `0.1.9` 尚未实现更新清单数字签名和资产 SHA-256 验证。Mac 开发不能把第 10.3 节当成已经存在的 Windows 接口。

共同版本 `0.2.0` 的目标是：

1. 保留现有 API → Atom Feed 的发现兼容流程。
2. 两个平台严格筛选自己的标签和架构资产。
3. Mac 在启动安装器前验证 Developer ID Installer 签名、Apple Team ID、包版本和目标路径。
4. 增加第 10.3 节的 Ed25519 签名更新清单；Windows `0.2.0` 同步实现验证，两个平台内置同一个公钥集合。
5. 签名密钥未配置时，CI 可以构建测试产物，但不得创建正式 Release。

### 10.1 平台通道

| 当前平台 | 正式标签 | 桥接兼容标签 | 更新资产 |
|---|---|---|---|
| Windows x64 | `win-v*` | `v0.1.8` 及更早 `v*` | `DSHLauncher-Windows-Update-<version>-x64.exe` |
| macOS arm64 | `mac-v*` | 无 | `DSHLauncher-Mac-Update-<version>-arm64.pkg` |
| macOS x64 | `mac-v*` | 无 | `DSHLauncher-Mac-Update-<version>-x64.pkg` |

旧 `v*` 只用于 Windows `0.1.8` 的迁移兼容。Mac 永远不能接受无平台前缀的旧标签。

### 10.2 检测顺序

1. 从配置的 GitHub 仓库读取 Release 列表。
2. GitHub REST API 可用时读取 Release、发布时间、draft/prerelease 和资产元数据；发生匿名 API 限流时使用官方 Releases Atom Feed，并按确定的资产名称构造下载地址。
3. 根据当前操作系统筛选标签：Windows 只保留 `win-v*`，Mac 只保留 `mac-v*`。
4. 使用严格正则从标签解析 SemVer，拒绝标签前缀错误、版本为空或格式异常的 Release。
5. 排除 draft；是否接受 prerelease 由 Launcher 的更新渠道设置决定，默认正式版只接受正式 Release。
6. 在候选 Release 中选择语义版本最高且高于当前 Launcher 的版本。Launcher 更新按 SemVer 判断；DSH 程序包更新仍按 DSH 官方 Release 发布时间判断，两者不能混用。
7. 根据 CPU 架构查找唯一匹配的更新资产，同时下载对应的平台更新清单和签名。
8. 用户确认后显示进度窗口并下载更新包。
9. 验证清单签名、资产 SHA-256、文件大小、标签版本、清单版本和资产文件名全部一致。
10. 调用当前平台的安装步骤。

### 10.3 统一更新清单

每个平台 Release 附带：

```text
update-manifest-windows-x64.json
update-manifest-windows-x64.sig
update-manifest-macos-arm64.json
update-manifest-macos-arm64.sig
update-manifest-macos-x64.json        # 提供 Intel 构建时
update-manifest-macos-x64.sig         # 提供 Intel 构建时
```

清单字段至少包含：

```json
{
  "schemaVersion": 1,
  "keyId": "release-2026-01",
  "platform": "macos",
  "architecture": "arm64",
  "tag": "mac-v0.2.0",
  "version": "0.2.0",
  "assetName": "DSHLauncher-Mac-Update-0.2.0-arm64.pkg",
  "assetSize": 12345678,
  "sha256": "...",
  "developerTeamId": "<APPLE_TEAM_ID>",
  "publishedAt": "2026-08-21T12:00:00Z"
}
```

清单按架构分别生成，避免一个文件与多个安装资产产生歧义。推荐使用 Ed25519：工作流对最终 JSON 文件的原始 UTF-8 字节签名，`.sig` 保存 Base64 编码的 64 字节签名。清单增加 `keyId`，Launcher 内置按 `keyId` 查找的公钥集合，以支持后续密钥轮换。

两个 Launcher 从 `0.2.0` 起内置同一套更新清单公钥。GitHub Actions 使用 `UPDATE_MANIFEST_PRIVATE_KEY` 对清单签名。私钥不进入源码、Release 资产或构建日志。普通 SHA-256 文件只能检查传输完整性，无法单独证明发布者身份；正式更新必须同时验证清单签名、平台代码签名和资产哈希。

### 10.4 平台安装步骤

共享逻辑到“下载与验证完成”为止，安装动作由平台适配器完成：

- **Windows**：启动轻量更新 EXE，退出 Launcher。更新器只替换 Launcher，不修改 Node、DSH 程序包、设置和 `DSH_HOME`。
- **macOS**：打开已签名、公证的轻量更新 `.pkg`，退出 Launcher。安装前再次使用系统工具验证 Developer ID Installer 签名；安装包只替换 `/Applications/DSH Launcher.app`。

安装器和更新器都不自动启动 DSH。用户正常启动新版 Launcher 后，应用清理自己的更新缓存。缓存清理必须先验证目标绝对路径位于 Launcher 缓存目录内。

### 10.5 失败处理

- GitHub API 限流：切换 Atom Feed，不能把限流错误直接显示为无法更新。
- 找到其他平台 Release：忽略，不报“有新版”。
- 标签正确但资产缺失或有多个匹配：停止更新并记录 Release URL。
- 下载中断：删除 `.download` 临时文件。写入成功后必须先关闭临时文件句柄，再执行重命名或移动。
- 标签版本、清单版本和应用版本不一致：拒绝安装。
- 清单签名、SHA-256 或平台代码签名失败：拒绝安装并保留当前 Launcher。
- 安装被用户取消：保留现有应用，下次启动可重新检查。

## 11. 日志、隐私和权限

日志路径：

```text
~/Library/Logs/DSHLauncher/launcher.log
```

日志可以记录：Launcher 版本、DSH 版本、进程 PID、端口、状态变化、命令退出码、Release 标签和脱敏后的路径。

日志禁止记录：API Key、`.credentials.yaml` 内容、完整会话内容、用户问题正文、审批输入参数、环境变量全集。

macOS 权限：

- 通知权限：首次需要通知时请求。
- 登录项：用户主动启用时通过 `SMAppService` 注册。
- 文件访问：用户通过系统选择器选中的 `DSH_HOME` 和工作目录可能需要保存 security-scoped bookmark，尤其在启用 App Sandbox 时。
- 管理员权限：只用于确实需要写入系统级 npm prefix 或 `/Applications` 的动作，每次显示明确原因。
- 建议初期使用 Hardened Runtime 且不启用 App Sandbox，因为 Launcher 需要启动任意工作目录中的 DSH、Node 和 npm；发布前仍需最小化 entitlements。

## 12. 测试与验收清单

### 12.1 干净机器安装

- [ ] Apple Silicon 干净用户环境下可安装签名、公证后的 `.pkg`。
- [ ] 没有 Node 时给出明确安装流程，安装后能正确发现 Node/npm。
- [ ] 默认全局安装 DSH，也能选择 Launcher 管理目录。
- [ ] `DSH_HOME` 默认是当前用户的 `~/.dsh`，可以手动选择其他目录。
- [ ] 安装过程不创建 root 所有的用户数据文件。

### 12.2 DSH 管理

- [ ] 能发现已有的 npm 全局 DSH。
- [ ] 能安装最新版和指定旧版本。
- [ ] 能同时保留多个独立版本并切换运行。
- [ ] 更新检查按官方 Release 发布时间选择更晚版本，不因 rc 字符串排序发生降级。
- [ ] 更新、重装和卸载程序包不会删除 `DSH_HOME`。
- [ ] 删除独立安装前完成归属和路径安全校验。

### 12.3 运行控制

- [ ] 启动命令带 `--no-open`，浏览器只打开一次。
- [ ] Web 服务只绑定 `127.0.0.1`。
- [ ] 启动失败显示有效 stderr，而不是只有 code 1。
- [ ] 停止和退出能结束 Launcher 自己的完整 DSH 进程组。
- [ ] 不会强制结束外部启动的 DSH。
- [ ] 端口占用时显示占用提示并允许修改端口。

### 12.4 状态和通知

- [ ] 停止、空闲、运行、等待处理图标正确。
- [ ] 提问和审批触发黄色闪烁与系统通知。
- [ ] 处理完成后停止闪烁。
- [ ] `turn/end` 发送节流后的完成通知。
- [ ] 点击通知能打开正确端口的 Web UI。
- [ ] WebSocket 断线后自动恢复且不重复轰炸通知。

### 12.5 Launcher 更新

- [ ] Mac 只发现 `mac-v*`，Windows 只发现 `win-v*`；两端同时校验平台标签和版本。
- [ ] Windows `0.1.8` 桥接后不再依赖 `/releases/latest`，Mac 从第一版起不依赖该地址。
- [ ] 下载窗口显示真实进度或循环进度。
- [ ] 更新清单签名、SHA-256 或平台安装包签名验证失败时拒绝安装。
- [ ] 更新后 DSH、`DSH_HOME` 和设置保持不变。
- [ ] 临时更新文件能自动清理。

### 12.6 GitHub 发布

- [ ] 旧 `v0.1.8` 只用于 Windows 桥接；此后 `win-v*` 只触发 Windows Release。
- [ ] `mac-v*` 只触发 macOS Release，并允许最后发布的 Mac Release 成为仓库 Latest。
- [ ] Release 资产名称包含平台、版本和架构。
- [ ] 代码签名、公证、staple、Gatekeeper 检查全部通过。
- [ ] Release 附带平台更新清单、清单数字签名和 SHA-256。

### 12.7 共享版本与回归样例

- [ ] Windows 与 Mac 的面向用户版本号一致，平台构建号可以不同。
- [ ] `win-vX.Y.Z` 与 `mac-vX.Y.Z` 指向同一发布基线，并只触发自己的工作流。
- [ ] GitHub API、Atom Feed、标签、资产名称和清单解析使用固定 JSON/XML 测试夹具。
- [ ] 同一组候选 Release 在 C# 与 Swift 测试中选择相同的平台版本。
- [ ] DSH 事件解析包含未知字段、重复事件、断线重连和过期事件样例。
- [ ] 临时目录、独立 DSH 目录和更新缓存的删除测试覆盖符号链接和越界路径。

## 13. 建议实施顺序

1. 按第 0.3 节创建 Swift 工程骨架、菜单栏应用、设置/日志基础设施和 `macos-ci.yml`。
2. 完成设置模型、路径、日志和配置窗口。
3. 完成 Node/npm 发现、全局 DSH 发现和独立版本安装。
4. 完成 DSH 进程启动、停止、就绪检测、端口检测和浏览器打开。
5. 完成多版本与多 `DSH_HOME` 管理。
6. 移植 WebSocket 状态机、图标状态和通知。
7. 完成 DSH Release 排序、更新、重装、卸载和进度窗口。
8. 接入 `SMAppService` 登录项和系统通知权限状态。
9. 接入统一 GitHub Release 检测协议和 Mac 轻量更新安装流程。
10. 增加 `macos-release.yml`，完成共享版本校验、更新清单签名、Developer ID 签名、公证和 Release 资产。
11. 在干净 Apple Silicon Mac 上按验收清单完成端到端测试。
12. 可选增加 Intel 构建和测试。

## 14. 已知风险

- DSH 仍处于开发者预览阶段，上游命令、事件和数据格式可能变化。所有协议解析应容忍未知字段，并对关键事件缺失提供日志。
- DSH 包含平台相关原生依赖。不同 CPU 架构的安装目录不可混用。
- Finder 启动的应用 PATH 与 Terminal 不同，Node 绝对路径和子进程 PATH 必须同时处理。
- npm 全局 prefix 的写权限因 Node 安装来源而异，不能默认所有 Mac 都能无管理员权限全局安装。
- 同一个 `DSH_HOME` 被新旧版本交替写入存在兼容风险。Launcher 只能提示，无法替上游保证数据兼容。
- GitHub 仓库级 Latest 只有一个，并跟随最后发布的平台版本。Windows `0.1.7` 及更早客户端可能错误读取跨平台 Latest，应先手动升级到支持平台标签的版本。
- 未签名或未公证的应用会被 Gatekeeper 阻止，正式公开发布前必须完成 Apple Developer ID 签名和公证。

## 15. Mac Codex 开始工作时的指令摘要

在 Mac 上克隆同一个仓库后：

1. 完整阅读本文件、根 `README.md`、`CHANGELOG.md` 和 Windows 的核心 Models/Services。
2. 保留现有 Windows 项目；Windows `0.1.9` 是历史基线，第一个双平台共同版本为 `0.2.0`。
3. 所有 Mac 源码放入 `macos/`。
4. 使用 Swift 6、SwiftUI + AppKit，优先实现 Apple Silicon、macOS 13+。
5. 保持 Launcher、DSH 程序包和 `DSH_HOME` 三者独立。
6. 不接触 API Key，不实现余额查询。
7. DSH 必须通过绝对 Node 路径启动，并设置正确的 `PATH`、`DSH_HOME`、`--host 127.0.0.1`、`--port` 和 `--no-open`。
8. Windows 和 Mac 共用产品版本号，分别使用 `win-v*`、`mac-v*` 标签；平台构建号和 Release 保持独立。
9. 两个平台的更新器都要校验平台标签和版本；仓库 Latest 跟随最后发布的平台 Release，不参与自动更新判断。
10. 每个实现阶段都运行测试；发布前完成签名、公证和干净机器验收。

## 16. 参考资料

- [DeepSeek Harness 官方仓库](https://github.com/deepseek-ai/deepseek-harness)
- [DeepSeek Harness 根 package.json（Node engines）](https://github.com/deepseek-ai/deepseek-harness/blob/master/package.json)
- [DeepSeek Harness 品牌使用规范](https://github.com/deepseek-ai/deepseek-harness/blob/master/BRAND_GUIDELINES.zh.md)
- [DSH CLI 说明](https://github.com/deepseek-ai/deepseek-harness/blob/master/apps/cli/README.md)
- [DSH CLI 行为参考](https://github.com/deepseek-ai/deepseek-harness/blob/master/apps/cli/reference/README.md)
- [DSH_HOME 路径说明](https://github.com/deepseek-ai/deepseek-harness/blob/master/packages/util/home-paths/README.md)
- [DSH 官方 Releases](https://github.com/deepseek-ai/deepseek-harness/releases)
- [Apple：Notarizing macOS software before distribution](https://developer.apple.com/documentation/security/notarizing-macos-software-before-distribution)
- [Apple：SMAppService](https://developer.apple.com/documentation/servicemanagement/smappservice)
- [GitHub Desktop 仓库](https://github.com/desktop/desktop)
- [Visual Studio Code 仓库](https://github.com/microsoft/vscode)
- [KeePassXC 仓库](https://github.com/keepassxreboot/keepassxc)
- [RustDesk 仓库](https://github.com/rustdesk/rustdesk)

# Support

Use GitHub Issues for reproducible Launcher bugs and feature requests.

For DeepSeek Harness behavior that also occurs when running `dsh` directly,
report it to the upstream DeepSeek Harness project. Never attach API keys,
`.credentials.yaml`, private DSH_HOME contents, or conversation data.
